# 프로젝트: 졸업논문 진행 관리 보드

## 목표

학생·교수·행정실 3개 역할 화면을 가진 웹앱. 화면 상세는 docs/의 초안 3개(student.md, professor.md, admin.md) 참고.
학과마다 졸업논문 절차가 다르므로, 절차는 코드에 고정하지 않고 학과별 절차 데이터로 정의한다.

## 기술 스택

- Next.js (App Router) + TypeScript + Tailwind CSS
- DB·백엔드·외부 API 없음. seed 데이터 + Zustand persist(localStorage)로 상태 공유
- API 키, 비밀번호, .env 사용 금지
- Vercel 기본 설정으로 빌드 가능해야 함
- 테스트: Vitest(단위), Playwright(E2E)

## 화면 구조

- 첫 화면: 해결하려는 문제 요약과 역할별 시작 버튼
- 상단 역할 전환 탭: 학생 / 교수 / 행정실 (로그인 없음, 데모 사용자 선택)
- 경로: /student, /professor, /admin
- 우측 상단 '데모 초기화' 버튼 (seed로 리셋)
- 학생 화면 첫 진입 시 전공 입력(주전공·복수전공 선택) → 전공별 로드맵 생성

## 학과별 절차 데이터

- 학과 데이터는 data/departments/\*.ts에 둔다
- 화면 컴포넌트에 특정 학과명·단계명을 하드코딩하지 말 것. 학과 데이터 파일 추가·교체만으로 동작해야 함
- 학과 데이터 필드:
  - 학과명, 적용 학기, 출처 구분(실제 공지 / 일부 가정 / 가상), 출처 설명, 확인일, 공식 링크
  - 논문 요건: 주전공·복수전공별 필수/면제
  - 지도교수 결정 방식:
    - contact\_approval: 학생이 교수에게 직접 컨택 후 승인 요청, 교수 승인
    - application: 시스템에서 신청, 교수 승인, 정원 적용
    - course\_assigned: 전공 수업에서 배정, 학생 신청 없음
  - 정원 사용 여부
  - 단계 목록: 단계명, 설명, 마감일, 제출 서류·서식, 제출 방법(시스템/이메일/수업), 증빙 방식, 유의사항, 담당 연락처
- 출처가 '가상' 또는 '일부 가정'인 학과는 화면에 '예시 데이터' 배지 표시
- 공식 링크는 사용자가 제공한 실제 주소만 사용하고 임의로 만들지 말 것. 실제 주소가 없으면 '예시 링크(가상)'로 표시
- 요건·마감일·공식 링크가 비어 있으면 화면에 '정보 확인 필요' 표시
- 실제 개인정보(실명, 개인 이메일·전화) 사용 금지. 인명과 연락처는 가상 값
- 나중에 docs/departments/\*.md에 실제 학과 정보가 추가되면, 그 내용으로 data/departments를 갱신한다

## 초기 학과 구성 (모두 가상 예시)

- 심리학과: contact\_approval, 정원 없음, 주전공·복수전공 모두 필수. 단계: 지도교수 확정 → 논문 작성 → 심사 제출 → 심사 결과 전달
- 기계공학과: course\_assigned, 주전공·복수전공 모두 필수. 단계: 졸업논문 수업 수강 → 지도교수 배정 확인 → 중간발표 → 최종논문 제출 → 성적 반영
- 사회학과: application, 정원 사용, 주전공 필수·복수전공 면제. 단계: 지도교수 신청 → 연구계획서 제출 → 중간발표 → 최종논문 제출 → 심사 완료
- 마감일은 2026-2학기 안의 가상 날짜

## 다전공 규칙

- 학생은 전공 목록(주전공, 복수전공)을 가짐
- 논문 요건이 '필수'인 전공마다 로드맵을 따로 생성. '면제' 전공은 면제로 표시
- 각 로드맵 상단에 요약 카드: 지도교수 결정 방식, 다음 마감, 제출 방법, 공식 링크
- 학생 화면: 전공별 로드맵 탭 + 모든 전공 마감을 합친 캘린더(전공별 색 구분)
- '다음 할 일'은 모든 전공 중 가장 급한 항목 1개를 전공명과 함께 표시
- 서로 다른 전공의 마감이 같은 주에 겹치면 경고 표시

## 도메인 규칙

- 신청 상태: 대기, 수정 요청, 면담 요청, 승인, 반려 (contact\_approval, application에 적용)
- contact\_approval: 학생이 '교수 컨택 완료'를 기록하고 승인 요청 → 교수 화면의 승인이 서명·이메일 증빙을 대체
- course\_assigned: 행정실이 배정하고 학생은 배정 결과만 확인
- 정원 규칙은 정원 사용 학과에만 적용:
  - 정원이 찬 교수에게는 학생이 신청할 수 없음 (신청 버튼 비활성, '정원 마감' 표시)
  - 남은 자리 0이면 교수 승인 버튼 비활성
  - 정원을 현재 배정 인원보다 작게 설정 불가
- 승인 시: 정원 사용 학과면 배정 인원 +1, 해당 전공 신청 상태 승인, 해당 전공 로드맵만 다음 단계로 이동
- 교수 승인 후 행정실이 '확정 검토'(검토 완료 / 보완 요청)를 처리하면 해당 전공의 지도교수 확정 단계가 완료됨
- 수정 요청·면담 요청·반려·보완 요청 시 피드백 입력 필수, 학생 화면에 즉시 표시
- 모든 상태 변경은 변경 이력(변경자, 시각, 내용)에 기록

## 작업 규칙

- 도메인 규칙은 lib/rules.ts에 순수 함수로 두고 Vitest로 테스트
- 작업 후 npm run build와 테스트 통과를 확인하고 보고
- 보고 형식: 변경 파일 / 구현 내용 / 확인 방법 / 남은 이슈
- 의미 있는 단위마다 git commit (한국어 메시지)
- UI 텍스트는 한국어
